[project]
name = "testcode-plugin"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = ["testcode-core>=0.1.0"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.uv]
dev-dependencies = ["pytest>=8", "pytest-asyncio>=0.24"]
